# git
To make git user friendly
# to install JAVA
```bash
sudo apt install openjdk-21-jdk-headless
```

#vi - Backspace

```bash
    vi ~/.vimrc
```
```bash
    set nocompatible
    set backspace=indent,eol,start
```
